export const metadata = {
  title: "AI Construstion Estimator",
  description: "AI estimate generator",
};
export default function Rootlayout({ children }) {
  return (
    <html lang="en">
    <body>{children}</body>
    </html>
  );
}
